# RT-Thread building script for RK3576 GMAC0

import os

from building import *

cwd = GetCurrentDir()
src = Glob('*.c')
CPPPATH = [cwd, os.path.join(cwd, '..')]

group = DefineGroup('Drivers', src, depend=['BSP_USING_GMAC0'], CPPPATH=CPPPATH)

Return('group')
